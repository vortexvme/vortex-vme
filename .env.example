# VME Manager URL (used by deploy.sh for Nginx proxy config)
# Example: https://your-vme-manager.example.com
VME_URL=https://your-vme-manager.example.com

# Dev-only: Vite proxy target (same as VME_URL for local dev)
# Leave empty in production (Nginx handles this)
VITE_API_BASE_URL=
